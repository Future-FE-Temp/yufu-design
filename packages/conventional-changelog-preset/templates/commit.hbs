{{~#if subScope}} 
* {{subScope}}
{{#each commits}}
 * {{~#if subject}}{{~subject}}{{~else}}{{~header}}{{~/if}}
{{/each}}
{{~else}}
{{~#each commits}}
* {{#if subject}}
  {{~subject}}
{{~else}}
  {{~header}}
{{~/if}}
{{/each}}
{{~/if}}

{{~> references root=@root}}